#!/usr/bin/env bash
#MISE description="Compare static and augmented text outputs"
#MISE depends=["build-static", "build-prose-if-enabled"]
#MISE sources=["dist/static/facts.txt", "dist/augmented/facts.txt"]
set -euo pipefail

diff -u "${STATIC_TXT:-dist/static/facts.txt}" "${AUGMENTED_TXT:-dist/augmented/facts.txt}" || true
